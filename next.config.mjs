/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  images: {
    domains: ['anerbktsogwtjpuzeetx.supabase.co']
  },
  
};

export default nextConfig;
